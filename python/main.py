from dotenv import load_dotenv
import os

load_dotenv()

from fetcher import get_places_review_number, get_places_routing_expenses

if __name__ == "__main__":
    # where program starts
    print("Start")

    # fetch places
    # check if places stored in database
    

    # gene algorithm

    # display results

